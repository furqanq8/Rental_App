# Port for the Express server
PORT=3000

# Optional absolute or relative path for the SQLite database file
# DATABASE_PATH=./data/azmat.sqlite3

# Optional comma separated list of allowed origins for API requests
# ALLOWED_ORIGINS=https://app.azmat.com,https://admin.azmat.com

# Authentication credentials for dashboard access
AZMAT_AUTH_USERNAME=admin
AZMAT_AUTH_PASSWORD=change-me

# Optional override for how long login sessions remain valid (in milliseconds)
# SESSION_TTL_MS=43200000
